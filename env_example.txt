# ==========================================
# Конфигурация Wednesday Frog Bot
# ==========================================
# Скопируйте этот файл в .env и заполните своими значениями
# cp env_example.txt .env
# ==========================================

# ===== ОБЯЗАТЕЛЬНЫЕ ПЕРЕМЕННЫЕ =====

# Токен Telegram бота (получите у @BotFather в Telegram)
# Формат: 1234567890:ABCdefGHIjklMNOpqrsTUVwxyz
TELEGRAM_BOT_TOKEN=your_bot_token_here

# API ключ Kandinsky (получите на https://fusionbrain.ai)
KANDINSKY_API_KEY=your_kandinsky_api_key_here

# Secret ключ Kandinsky (получите на https://fusionbrain.ai)
KANDINSKY_SECRET_KEY=your_kandinsky_secret_key_here

# ID чата или канала для отправки сообщений
# Для личного чата: ваш ID (получите от @userinfobot)
# Для группы: ID группы (обычно начинается с -)
# Для канала: ID канала (обычно начинается с -100)
CHAT_ID=your_chat_or_channel_id_here

# ===== НАСТРОЙКИ ПЛАНИРОВЩИКА =====

# Времена отправки в среду (через запятую)
# Можно указать несколько времен, например: 09:00,12:00,18:00
SCHEDULER_SEND_TIMES=09:00,12:00,18:00

# Часовой пояс для расписания
# Список: https://en.wikipedia.org/wiki/List_of_tz_database_time_zones
# По умолчанию: Europe/Moscow
SCHEDULER_TZ=Europe/Moscow

# День недели для отправки (по умолчанию среда)
# 0 = понедельник, 1 = вторник, 2 = среда, 3 = четверг, 4 = пятница, 5 = суббота, 6 = воскресенье
SCHEDULER_WEDNESDAY_DAY=2


# ===== ЛОГИРОВАНИЕ =====

# Уровень логирования
# Доступные: DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_LEVEL=INFO

# ===== API НАСТРОЙКИ =====

# Таймаут для генерации изображения в секундах
# Увеличьте если изображения генерируются долго
GENERATION_TIMEOUT=60

# Максимальное количество попыток генерации изображения
# При неудаче бот попробует еще раз
MAX_RETRIES=3

# ===== RETRY-МЕХАНИКИ ДЛЯ HTTP-ЗАПРОСОВ =====

# Максимальное количество попыток для retry-механик HTTP-запросов
# Используется для автоматических повторных попыток при сетевых ошибках
# По умолчанию: 5
RETRY_MAX_ATTEMPTS=5

# Настройки retry для разных типов операций
# Количество попыток для стандартных операций (по умолчанию: 3)
RETRY_STANDARD_MAX_ATTEMPTS=3
# Количество попыток для критичных операций (по умолчанию: 5 или RETRY_MAX_ATTEMPTS)
RETRY_CRITICAL_MAX_ATTEMPTS=5
# Количество попыток для необязательных операций (по умолчанию: 2)
RETRY_OPTIONAL_MAX_ATTEMPTS=2

# Множитель для экспоненциального backoff
# Время ожидания между попытками = min_wait * (multiplier ^ (attempt - 1))
# По умолчанию: 1.0
RETRY_MULTIPLIER=1.0

# Минимальное время ожидания между попытками в секундах
# По умолчанию: 2.0
RETRY_MIN_WAIT=2.0

# Максимальное время ожидания между попытками в секундах
# По умолчанию: 30.0
RETRY_MAX_WAIT=30.0

# ===== CIRCUIT BREAKER (защита от перегрузки внешних API) =====

# Количество ошибок до открытия circuit breaker
# По умолчанию: 5
CIRCUIT_BREAKER_THRESHOLD=5

# Окно жизни счётчика ошибок в секундах
# По умолчанию: 300
CIRCUIT_BREAKER_WINDOW=300

# Минимальный интервал после последней ошибки в секундах, в течение которого circuit считается открытым
# Опционально, по умолчанию равен CIRCUIT_BREAKER_WINDOW
# CIRCUIT_BREAKER_COOLDOWN=300

# ===== REDIS (опционально, для кэша и временного состояния) =====

# Полный URL подключения к Redis (если задан, перекрывает host/port/db/password)
# Примеры:
# REDIS_URL=redis://localhost:6379/0
# REDIS_URL=redis://:password@localhost:6379/0 (с паролем)
# REDIS_URL=rediss://user:pass@host:6380/1 (с TLS)
REDIS_URL=

# Альтернативная форма задания подключения, если REDIS_URL не используется:
# Хост Redis
# Для локальной разработки: localhost
# Для docker-compose: redis (имя сервиса в docker-compose.yml)
REDIS_HOST=localhost

# Порт Redis (по умолчанию 6379)
REDIS_PORT=6379

# Номер базы данных Redis (по умолчанию 0)
REDIS_DB=0

# Пароль для аутентификации в Redis (опционально)
# Если указан, Redis будет требовать пароль при подключении
# ОБЯЗАТЕЛЬНО установите пароль в продакшн окружении!
REDIS_PASSWORD=

# ===== GIGACHAT НАСТРОЙКИ (для генерации промптов) =====

# Authorization key для GigaChat API (Base64 encoded)
# Получите на https://developers.sber.ru/ (нужна регистрация)
# Формат: Base64 строка (например: R1JhbnQ6...)
# ВАЖНО: Это ключ для генерации промптов через нейросеть GigaChat
# Если не указано, будет использоваться fallback на статические промпты
GIGACHAT_AUTHORIZATION_KEY=

# URL для аутентификации GigaChat (опционально, по умолчанию используется стандартный)
# GIGACHAT_AUTH_URL=https://ngw.devices.sberbank.ru:9443/api/v2/oauth

# URL для API запросов GigaChat (опционально, по умолчанию используется стандартный)
# GIGACHAT_API_URL=https://gigachat.devices.sberbank.ru/api/v1/chat/completions

# Scope для аутентификации (опционально, по умолчанию: GIGACHAT_API_PERS)
# GIGACHAT_SCOPE=GIGACHAT_API_PERS

# Модель GigaChat для использования (опционально, по умолчанию: GigaChat)
# GIGACHAT_MODEL=GigaChat

# Путь к файлу сертификата для GigaChat API (опционально)
# Если указан, будет использоваться этот сертификат вместо стандартных
# Пример: certs/russiantrustedca.pem или /путь/к/сертификату.pem
# Приоритет: если указан GIGACHAT_CERT_PATH, он используется; иначе проверяется GIGACHAT_VERIFY_SSL
# GIGACHAT_CERT_PATH=certs/russiantrustedca.pem

# Проверять SSL сертификаты для GigaChat (опционально, по умолчанию: true)
# Используется только если GIGACHAT_CERT_PATH не указан
# Установите false если есть проблемы с сертификатами и сертификат не указан
# ВНИМАНИЕ: Отключение проверки SSL снижает безопасность!
# GIGACHAT_VERIFY_SSL=true

# ===== АДМИН НАСТРОЙКИ =====

# ID администратора для доступа к админ-командам (опционально)
# Если не указано, админ-команды будут доступны всем
# Получите ваш ID от @userinfobot
ADMIN_CHAT_ID=

# ===== ПРОКСИ (не реализовано) =====

# Если нужен прокси для доступа к Telegram Bot API
# Вариант 1: Прямой HTTP/HTTPS/SOCKS прокси (рекомендуется, если доступен)
# Примеры:
# TELEGRAM_PROXY_URL=http://user:pass@host:port
# TELEGRAM_PROXY_URL=https://user:pass@host:port
# TELEGRAM_PROXY_URL=socks5://127.0.0.1:1080
TELEGRAM_PROXY_URL=

# Вариант 2: VLESS через локальный прокси (Xray/V2Ray)
# Укажите строку VLESS (для справки/логов) — напрямую не используется PTB/HTTPX
# Пример: vless://<uuid>@host:port?encryption=none&security=reality#name
TELEGRAM_VLESS_URL=
# Укажите адрес локального HTTP/SOCKS прокси, который поднимает Xray/V2Ray поверх VLESS
# Примеры:
# TELEGRAM_VLESS_PROXY=http://127.0.0.1:8080
# TELEGRAM_VLESS_PROXY=socks5://127.0.0.1:1080
TELEGRAM_VLESS_PROXY=

# Альтернативно можно задать системные переменные прокси для других клиентов
# HTTPS_PROXY=http://proxy-server:port
# HTTP_PROXY=http://proxy-server:port

# ===== ТЕСТОВЫЙ РЕЖИМ (опционально) =====

# Запуск генерации каждые N минут (для тестирования)
# Установите 0 для отключения, или число минут для тестирования
# Например, 10 = каждые 10 минут
SCHEDULER_TEST_MINUTES=0

# ===== POSTGRES НАСТРОЙКИ =====

# Имя пользователя для подключения к PostgreSQL
# По умолчанию используется при создании БД в docker-compose
POSTGRES_USER=your_postgres_user

# Пароль пользователя PostgreSQL
# ОБЯЗАТЕЛЬНО замените на безопасный пароль в продакшн окружении!
POSTGRES_PASSWORD=your_postgres_password

# Имя базы данных
POSTGRES_DB=your_postgres_db

# Хост PostgreSQL
# Для локальной разработки: localhost
# Для docker-compose: postgres (имя сервиса в docker-compose.yml)
POSTGRES_HOST=localhost

# Порт PostgreSQL (по умолчанию 5432)
POSTGRES_PORT=5432

# ===== SENTRY МОНИТОРИНГ ОШИБОК (опционально) =====

# DSN для интеграции с Sentry (Data Source Name)
# Получите на https://sentry.io/ после создания проекта
# Формат: https://<key>@<organization>.ingest.sentry.io/<project_id>
# Пример: https://abc123def456@o1234567.ingest.sentry.io/1234567
# Если не указано, Sentry будет отключён (ошибки не будут отправляться в Sentry)
SENTRY_DSN=

# Название окружения для Sentry (опционально)
# Помогает различать ошибки в разных окружениях
# Примеры: production, staging, development, local
SENTRY_ENVIRONMENT=

# Версия релиза для Sentry (опционально)
# Может быть git-хэш, семантическая версия или любое другое значение
# Примеры: v1.0.0, abc123def456, 1.2.3
# Рекомендуется устанавливать через CI/CD при деплое
RELEASE=

# ============================================
# CELERY CONFIGURATION
# ============================================

# Celery Broker и Backend (Redis URL)
# Используется тот же Redis, что и для основного приложения
# CELERY_BROKER_URL=redis://localhost:6379/0
# CELERY_BACKEND_URL=redis://localhost:6379/0

# Timezone для Celery Beat
# CELERY_TIMEZONE=Europe/Amsterdam
# SCHEDULER_TZ=Europe/Amsterdam

# Worker настройки
# WORKER_CONCURRENCY=8
# WORKER_PREFETCH_MULTIPLIER=1
# CELERY_ASYNC_MODE=asyncio

# Beat настройки
# BEAT_ENABLED=1
# BEAT_MAX_LOOP_INTERVAL=10

# Retry настройки
# CELERY_TASK_MAX_RETRIES=3
# CELERY_TASK_RETRY_BACKOFF=True
# CELERY_TASK_RETRY_BACKOFF_MAX=600
# CELERY_TASK_RETRY_JITTER=True

# Dead Letter Queue
# CELERY_TASK_REJECT_ON_WORKER_LOST=True
# CELERY_TASK_ACKS_LATE=True
# CELERY_DLQ_ENABLED=1
# CELERY_DLQ_QUEUE=dead_letter

# Queue concurrency (разделение по очередям)
# CELERY_QUEUE_WEDNESDAY_CONCURRENCY=6
# CELERY_QUEUE_IMAGES_CONCURRENCY=2
# CELERY_QUEUE_MAINTENANCE_CONCURRENCY=1

# ==========================================
# ПРИМЕЧАНИЯ
# ==========================================
# - Никогда не коммитьте файл .env в Git
# - Проверьте права доступа к файлу .env (chmod 600 .env)
# - После изменения .env перезапустите бота
# - Используйте кавычки если в значениях есть пробелы
# ==========================================
