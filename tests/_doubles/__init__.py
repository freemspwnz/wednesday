"""
Тестовые дублёры (doubles) для сервисов.

Пакет `tests._doubles` содержит простые синтетические реализации
интерфейсов (моки/фейковые клиенты), которые используются в юнит‑тестах
для изоляции бизнес‑логики от внешних API и инфраструктуры.

Важно: каталог `tests/_doubles` является полноценным пакетом с `__init__`,
чтобы mypy не видел его как корневой `_doubles` и не дублировал модули.
"""
